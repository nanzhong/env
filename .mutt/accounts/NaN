# Account Settings ------------------------------
set from     = "nan@notanumber.io"
set sendmail = "/usr/local/bin/msmtp -a NaN"

# Default inbox.
set spoolfile = "+NaN/INBOX"

# Other special folders.
set mbox      = "+NaN/[Gmail].All Mail"
set postponed = "+NaN/[Gmail].Drafts"
set record    = "+NaN/[Gmail].Sent Mail"

color status brightgreen default
color indicator brightblack brightgreen
color progress brightblack brightgreen
