# Account Settings ------------------------------
set from     = "nzhong@digitalocean.com"
set sendmail = "/usr/local/bin/msmtp -a do"

# Default inbox.
set spoolfile = "+do/INBOX"

# Other special folders.
set mbox      = "+do/[Gmail].All Mail"
set postponed = "+do/[Gmail].Drafts"
unset record

color status brightblue default
color indicator brightblack brightblue
color progress brightblack brightblue
